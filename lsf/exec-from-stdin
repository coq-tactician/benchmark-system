#!/bin/bash
read location
cd $location
finished=false
while ! $finished; do
    read envvar
    # At some point
    if [ $envvar = "env-done" ]
    then
        finished=true
    else
        eval "$envvar"
    fi
done
read command
eval "exec $command"
